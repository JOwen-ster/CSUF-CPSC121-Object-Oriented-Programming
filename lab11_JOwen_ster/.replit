run = "./run"

hidden = ["main", "**/*.o", "**/*.d", ".ccls-cache", "Makefile", ".clang-format"]

[languages.cpp]
pattern = "**/*.{cc,h}"
[languages.cpp.languageServer]
start = "clangd"

[debugger]
support = true

[nix]
channel = "stable-22_05"
